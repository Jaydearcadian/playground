# Mapping Rules

Dune layer is witness-only:
- validates counts and diagnostics
- does not replace canonical root/proof verification

Each query output should explicitly map to one bundle field or derived summary metric.
